import { Command } from '@/types';

const cancel: Command = {
  name: 'cancel',
  aliases: [],
  usage: 'cancel',
  description: 'cancel',
  async execute() {
    // TODO 製作取消通知的指令
  },
};

export default cancel;
